/**
 * This is the config file, this is where all important variables are defined
 */

const mainUrl = 'https://pokeapi.co/api/v2/';

/**
 * url to pokeapi's pokemon endpoint
 */
export const pokeUrl = `${mainUrl}pokemon/`;

/**
 * url to pokeapi's item endpoint
 */
export const itemUrl = `${mainUrl}item/`;

/**
 * div element to hold the cards
 */
export const div = document.getElementById('div');