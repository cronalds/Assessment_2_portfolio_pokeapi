const mainUrl = 'https://pokeapi.co/api/v2/';

/**
 * url to the api pokeapi's pokemon endpoint
 */
export const pokeUrl = `${mainUrl}pokemon/`;

/**
 * url to the api pokeapi's berry endpoint
 */
export const berryUrl = `${mainUrl}berry/`;
/**
 * url to the api pokeapi's item endpoint
 */
export const itemUrl = `${mainUrl}item/`;